<section class="small-10 columns">
<h1>Select Department</h1>

<% @departments.each do |department| %>
    <div>
	    <%= link_to department.name, department_courses_path(department) %>
	    <%= department.school %>
    </div>
    <br>
<% end %>
</section>
